# Nhost / Hasura migration history

This folder is reserved for Nhost/Hasura-managed migration files.

Current repo schema history still lives in `packages/db/migrations`.
Use `docs/nhost-baseline-cutover-checklist.md` for the one-time baseline cutover process to move to Nhost-managed migration application.
