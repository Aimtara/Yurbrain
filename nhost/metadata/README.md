# Nhost / Hasura metadata

This folder is reserved for Hasura metadata exports used by Nhost deploy workflows.

Populate metadata as part of the baseline cutover described in `docs/nhost-baseline-cutover-checklist.md`.
